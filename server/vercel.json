{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ],
  "functions": {
    "server.ts": {
      "runtime": "nodejs24.x",
      "maxDuration": 10,
      "memory": 1024
    }
  },
  "note": "Vercel uses serverless functions — persistent WebSocket or long-running servers (like your socketServer) are not supported. Consider deploying sockets elsewhere (Render, Fly, Heroku, etc.) and using environment variables to point to it."
}